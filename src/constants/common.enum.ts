export enum EDIT_ABLE_MODE {
  ON = 'ON',
  OFF = 'OFF',
}

export enum FILE_TYPES {
  PDF = 'PDF',
  DOC = 'DOC',
  DOCX = 'DOCX',
}

export enum INFINITE_ITEM_STATUS {
  LOADING = 'LOADING',
  LOADED = 'LOADED',
}
